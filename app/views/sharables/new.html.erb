<h1>New Sharables</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= form_for(@sharable) do |f| %>
  <% if @sharable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sharable.errors.count, "error") %> prohibited this file from being saved:</h2>

      <ul>
      <% @sharable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :file %><br>
    <%= f.file_field :file, class: 'form-control' %>

    <%= f.submit "Upload", class: "btn btn-primary" %>

<% end %>
</div>
</div>
